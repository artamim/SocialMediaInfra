apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: fastapi-app
  ports:
    - protocol: TCP
      port: 8000       # Service port inside cluster
      targetPort: 8000 # Container port
      nodePort: 30080  # External port on EC2 node